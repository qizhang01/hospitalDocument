module.exports = {
    extends: [
      'eslint:recommended',
      'plugin:vue/essential',
      'plugin:prettier/recommended' // 在最后添加这一行以启用ESLint与Prettier的集成
    ],
    rules: {
      // 在这里添加或覆盖规则
    }
  };